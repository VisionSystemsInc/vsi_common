#!/usr/bin/env bash

. "$(dirname "${BASH_SOURCE[0]}")/testlib.bsh"
VSI_COMMON_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.."; pwd)"

TESTLIB_STOP_AFTER_FAILS=1

begin_test "git_mirror"
(
  # use TRASHDIR between tests
  setup_test

  echo 1
)
end_test

begin_test "git_mirror2"
(
  # use TRASHDIR between tests
  setup_test

  false
)
end_test

begin_test "git_mirror3"
(
  # use TRASHDIR between tests
  setup_test

  echo 1
)
end_test