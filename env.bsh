#!/usr/bin/env bash

#A simple script for you to source to auto use this vsi common environment

VSI_COMMON_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")"; pwd)"

export PYTHONPATH="${VSI_COMMON_DIR}/python"${PYTHONPATH:+:"${PYTHONPATH}"}
export PATH="${VSI_COMMON_DIR}/linux"${PATH:+:"${PATH}"}
export MATLABPATH="${VSI_COMMON_DIR}/matlab"${MATLABPATH:+:"${MATLABPATH}"}

. ${VSI_COMMON_DIR}/linux/.just