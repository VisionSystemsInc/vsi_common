#!/usr/bin/env bash

#A simple script for you to source to auto use this vsi common environment

BASE_DIR=$(cd $(dirname ${BASH_SOURCE[0]}); pwd)

export PYTHONPATH=${PYTHONPATH:+${PYTHONPATH}:}${BASE_DIR}/python
export PATH=${PATH:+${PATH}:}${BASE_DIR}/bash
export MATLABPATH=${MATLABPATH:+${MATLABPATH}:}${BASE_DIR}/matlab

