#!/usr/bin/env bash

#A simple script for you to source to auto use this vsi common environment

VSI_COMMON_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")"; pwd)

export PYTHONPATH=${PYTHONPATH:+${PYTHONPATH}:}${VSI_COMMON_DIR}/python
export PATH=${PATH:+${PATH}:}${VSI_COMMON_DIR}/linux
export MATLABPATH=${MATLABPATH:+${MATLABPATH}:}${VSI_COMMON_DIR}/matlab

